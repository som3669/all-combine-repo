/**
 * The only file you need to edit to repoint or rebrand the page.
 *
 * lockerUrl is the CPAGrip-hosted content locker. The page appends a per-visitor
 * tracking_id to it so the Worker's postback can attribute the conversion back here.
 */

window.RV_CONFIG = {
  lockerUrl: 'https://singingfiles.com/show.php?l=0&u=107151&id=75202',

  // Shown in the headline and the CTA. Keep it to what you actually deliver.
  rewardName: '$25 gift card code',

  // Set to a path or URL to hand out the reward yourself once a conversion lands.
  // Left null, the page just tells the visitor the advertiser has to confirm first.
  rewardUrl: null,
};
